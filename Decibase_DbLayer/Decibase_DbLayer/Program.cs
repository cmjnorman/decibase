using Microsoft.EntityFrameworkCore;
using System;
using System.Linq;

namespace Decibase_DbLayer
{
    class Program
    {
        static void Main(string[] args)
        {
            //using(var db = new BloggingContext())
            //{
            //    //db.Add(new Track { Title = "Song 1" });
            //    //db.Add(new Artist { Name = "Artist 1" });
            //    //var track = db.Tracks.Where(t => t.Title == "Song 1").Include(t => t.Artists).FirstOrDefault();
            //    //var artist = db.Artists.Where(a => a.Name == "Artist 1").Include(a => a.Tracks).FirstOrDefault();
            //    //track.Artists.Add(artist);
            //    //artist.Tracks.Add(track);
            //}

        }
    }
}
